[pytest]
pythonpath = .
markers =
    unit: pure unit tests for gameplay logic with no pygame dependencies
    pygame: tests that require pygame initialization or surfaces
    levels: tests that exercise multi-level progression and maze cycling
testpaths = tests
